<%= form_for(@scrapboard) do |f| %>
  <% if @scrapboard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scrapboard.errors.count, "error") %> prohibited this scrapboard from being saved:</h2>

      <ul>
      <% @scrapboard.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
